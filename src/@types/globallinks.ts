export interface Link {
    secret_id: string;
    title: string;
    imgurl: string;
    tobefind: boolean;
    description: string;
    links: string;
}

export interface GlobalLinks {
    secret_id: string;
    title: string;
    imgurl: string;
    tobefind: boolean;
    description: string;
    links: string;
}